# Docker Compose: path do host a ser escaneado pelo backend.
# Ao criar um projeto no TestForge, use o path como visto no container (ex: /workspace ou /workspace/meu-projeto).
PROJECTS_PATH=.
CONTAINER_WORKSPACE=/workspace

# Se os projetos foram criados com path do host (ex: /Users/voce/Documents/Projetos/meu-projeto), defina o mapeamento
# para o engine encontrar o projeto no container: prefixo do host → prefixo no container.
PROJECT_PATH_HOST_PREFIX=
PROJECT_PATH_CONTAINER_PREFIX=/workspace
